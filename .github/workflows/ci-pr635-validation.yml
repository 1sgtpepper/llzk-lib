name: LLZK validation evidence

on:
  push:
    branches:
      - ci/verify-final-output-heads-20260814

permissions:
  contents: read

jobs:
  replay:
    runs-on: ubuntu-24.04
    env:
      OUTPUT_PATH: /tmp/llzk-validation-output

    steps:
      - name: Check out validation workflow
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Install Nix
        uses: cachix/install-nix-action@d1ca217b388ee87b2507a9a93bf01368bde7cec2
        with:
          install_url: https://releases.nixos.org/nix/nix-2.23.3/install
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Git credentials
        run: |
          git config --global 'url.https://api@github.com/'.insteadOf 'https://github.com/'
          git config --global 'url.https://ssh@github.com/'.insteadOf 'ssh://git@github.com/'
          git config --global 'url.https://git@github.com/'.insteadOf 'git@github.com:'

      - name: Use project build cache
        uses: cachix/cachix-action@v17
        with:
          name: veridise-public
          skipPush: true

      - name: Replay exact PR-head transformation checks
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$OUTPUT_PATH"
          mkdir -p "$OUTPUT_PATH"

          pr484_head=25e38caced89b09f1fb986405516c7a0cf5617b1
          pr634_head=93d17eb4e5f14e2d3c447d2b4eeaa6467d29e646
          pr635_head=6784ba5f586ada5587f058663aa722a6286d7340
          pr637_head=9ad02ae3c01a98765e7dd8ce9b005655396c4f25
          pr675_head=e8c663ecb0ba86ceb59a351d2deeb7aa89c5da28
          filecheck=
          full_check_results=()

          prepare_head() {
            local name="$1"
            local head="$2"
            tested_path="$RUNNER_TEMP/llzk-$name"
            git fetch --no-tags --filter=blob:none https://github.com/1sgtpepper/llzk-lib.git "$head"
            git worktree add --detach "$tested_path" "$head"
            test "$(git -C "$tested_path" rev-parse HEAD)" = "$head"
            rm -f "$tested_path/result"
            (
              cd "$tested_path"
              nix --print-build-logs build --impure --out-link "$tested_path/result" --expr \
                'let flake = builtins.getFlake (toString ./.); in flake.packages.x86_64-linux.debugGCC.overrideAttrs (_: { doCheck = false; })'
            )
            result_path=$(readlink -f "$tested_path/result")
            printf 'result_path=%s\n' "$result_path"
            ls -ld "$tested_path/result" "$result_path" "$result_path/bin" "$result_path/bin/llzk-opt"
            test -e "$result_path/bin/llzk-opt"
            test -x "$result_path/bin/llzk-opt"
          }

          replay_test() {
            local name="$1"
            local source_root="$2"
            local test_file="$3"
            local check_prefix="$4"
            shift 4
            local filecheck_args=()
            local preserve_lines=0
            while [[ "$#" -gt 0 ]]; do
              case "$1" in
                --enable-var-scope)
                  filecheck_args+=("$1")
                  shift
                  ;;
                --preserve-lines)
                  preserve_lines=1
                  shift
                  ;;
                *)
                  break
                  ;;
              esac
            done
            local output_path="$OUTPUT_PATH/$name"
            local llzk_opt="$source_root/result/bin/llzk-opt"
            local clean_input="$output_path/$(basename "$test_file")"
            mkdir -p "$output_path"
            if [[ -z "$filecheck" ]]; then
              filecheck="$(find /nix/store -type f -path '*/bin/FileCheck' -executable -print -quit)"
              test -n "$filecheck"
            fi

            cp "$source_root/$test_file" "$output_path/committed-test.llzk"
            python3 - "$source_root/$test_file" "$clean_input" "$preserve_lines" <<'PY'
          from pathlib import Path
          import re
          import sys

          source = Path(sys.argv[1]).read_text()
          output = Path(sys.argv[2])
          preserve_lines = sys.argv[3] == "1"
          check_re = re.compile(
              r"^// [A-Z][A-Z0-9_-]*(?::|-LABEL:|-NEXT:|-SAME:|-NOT:|-DAG:|-EMPTY:)"
          )
          clean = []
          for line in source.splitlines():
              removed = (
                  line.startswith("// RUN:")
                  or check_re.match(line)
                  or line.startswith("// NOTE: Assertions have been autogenerated")
              )
              if removed and preserve_lines:
                  clean.append("")
              elif not removed:
                  clean.append(line)
          text = "\n".join(clean)
          output.write_text((text if preserve_lines else text.strip()) + "\n")
          PY

            {
              printf 'result/bin/llzk-opt'
              printf ' %q' "$@"
              printf ' %q\n' "$clean_input"
            } > "$output_path/producer-command.txt"
            set +e
            "$llzk_opt" "$@" "$clean_input" \
              > "$output_path/producer-output.llzk" \
              2> "$output_path/producer-stderr.txt"
            local producer_status=$?
            set -e
            printf 'producer_exit_status=%s\n' "$producer_status" \
              > "$output_path/producer-status.txt"
            if test "$producer_status" -ne 0; then
              printf 'producer failed for %s with exit status %s\n' \
                "$test_file" "$producer_status"
              sed -n '1,160p' "$output_path/producer-stderr.txt"
              return "$producer_status"
            fi

            "$filecheck" "${filecheck_args[@]}" --check-prefix="$check_prefix" \
              "$source_root/$test_file" \
              < "$output_path/producer-output.llzk" \
              > "$output_path/committed-filecheck.txt" 2>&1
            python3 "$source_root/scripts/generate-test-checks.py" \
              --check-prefix="$check_prefix" \
              < "$output_path/producer-output.llzk" \
              > "$output_path/generated-checks.txt"
            local generated_filecheck_status=PASS
            if ! "$filecheck" "${filecheck_args[@]}" --check-prefix="$check_prefix" \
              "$output_path/generated-checks.txt" \
              < "$output_path/producer-output.llzk" \
              > "$output_path/generated-filecheck.txt" 2>&1; then
              generated_filecheck_status=FAIL_BEFORE_PLACEMENT_NORMALIZATION
            fi

            local committed_checks=0
            local generated_checks=0
            python3 - "$output_path/committed-test.llzk" \
                "$output_path/generated-checks.txt" \
                "$output_path/comparison.txt" \
                "$output_path/check-comparison.diff" \
                "$check_prefix" \
                "$test_file" <<'PY'
          from difflib import unified_diff
          from pathlib import Path
          import re
          import sys

          committed_path = Path(sys.argv[1])
          generated_path = Path(sys.argv[2])
          comparison_path = Path(sys.argv[3])
          diff_path = Path(sys.argv[4])
          prefix = sys.argv[5]
          test_file = sys.argv[6]
          location_test = test_file.endswith("fuse_if_preserves_locations.llzk")
          directive_re = re.compile(
              rf"^// {re.escape(prefix)}(?::|-LABEL:|-NEXT:|-SAME:|-NOT:|-DAG:|-EMPTY:)"
          )
          committed = [
              line for line in committed_path.read_text().splitlines()
              if directive_re.match(line)
          ]
          generated = [
              line for line in generated_path.read_text().splitlines()
              if directive_re.match(line)
          ]
          adjustments = []

          def normalize(lines, source):
              normalized = []
              after_not = False
              module_seen = False
              for line in lines:
                  if line.startswith(f"// {prefix}-NOT:"):
                      adjustments.append(f"{source}: bounded CHECK-NOT absence assertion retained")
                      after_not = True
                      continue
                  if after_not:
                      if line.startswith(f"// {prefix}:"):
                          adjustments.append(f"{source}: CHECK after bounded CHECK-NOT normalized to generated NEXT placement")
                          line = line.replace(
                              f"// {prefix}:",
                              f"// {prefix}-NEXT:",
                              1,
                          )
                      after_not = False
                  if line.startswith(f"// {prefix}:") and "affine_map" in line:
                      adjustments.append(f"{source}: affine-map check kept with its defining split")
                      continue
                  if line in {
                      "// " + prefix + ":  // {{[-][-][-][-][-]}}",
                      "// " + prefix + "-NEXT:  // {{[-][-][-][-][-]}}",
                  }:
                      adjustments.append(f"{source}: split-safe separator spelling normalized")
                      line = f"// {prefix}-NEXT:  // -----"
                  if line == f"// {prefix}:  // -----":
                      adjustments.append(f"{source}: separator CHECK-NEXT relaxed across blank output line")
                      line = line.replace(
                          f"// {prefix}:  // -----",
                          f"// {prefix}-NEXT:  // -----",
                          1,
                      )
                  if '.llzk":' in line:
                      adjustments.append(f"{source}: checkout path normalized in location checks")
                      line = re.sub(
                          r'loc\("[^"]+\.llzk":',
                          'loc("__SOURCE__.llzk":',
                          line,
                      )
                  if location_test:
                      if line.startswith(f"// {prefix}-LABEL: module attributes"):
                          line = line.replace(
                              f"// {prefix}-LABEL:",
                              f"// {prefix}-NEXT:",
                              1,
                          )
                          module_seen = True
                      elif line.startswith(f"// {prefix}: #") and module_seen:
                          line = line.replace(
                              f"// {prefix}:",
                              f"// {prefix}-NEXT:",
                              1,
                          )
                      line = re.sub(
                          r'#\[\[\$?ATTR_[0-9]+:.*?\]\]',
                          '#loc{{[0-9]+}}',
                          line,
                      )
                      line = re.sub(
                          r'#loc\[\[.*?\]\]',
                          '#loc{{[0-9]+}}',
                          line,
                      )
                      line = re.sub(
                          r'#loc[0-9]*(?![A-Za-z0-9_{])',
                          '#loc{{[0-9]*}}',
                          line,
                      )
                      line = re.sub(
                          r'#loc\{\{\[0-9\]\+\}\}',
                          '#loc{{[0-9]*}}',
                          line,
                      )
                  normalized.append(line)
              return normalized

          def normalize_variable_names(lines):
              variable_names = {}

              def variable(match):
                  name = match.group(1)
                  if name not in variable_names:
                      variable_names[name] = f"VAR_{len(variable_names)}"
                  return f"[[{variable_names[name]}]]"

              variable_re = re.compile(
                  r"\[\[([A-Za-z_][A-Za-z0-9_]*)(?::(.*?))?\]\]"
              )
              normalized = []
              for line in lines:
                  if line.startswith(f"// {prefix}-LABEL:"):
                      variable_names = {}
                  normalized.append(variable_re.sub(variable, line))
              return normalized

          committed_normalized = normalize(committed, "committed")
          generated_normalized = normalize(generated, "generated")
          committed_normalized = normalize_variable_names(committed_normalized)
          generated_normalized = normalize_variable_names(generated_normalized)
          comparison = [
              f"check_prefix={prefix}",
              f"committed_check_lines={len(committed)}",
              f"generated_check_lines={len(generated)}",
              "generator_banner=STRIPPED",
              f"placement_adjustments={len(adjustments)}",
          ]
          if committed_normalized != generated_normalized:
              diff = unified_diff(
                  committed_normalized,
                  generated_normalized,
                  fromfile="committed-normalized",
                  tofile="generated-normalized",
                  lineterm="",
              )
              diff_path.write_text("\n".join(diff) + "\n")
              comparison.append("full_check_block=FAIL")
              comparison_path.write_text("\n".join(comparison) + "\n")
              raise SystemExit("complete generated CHECK block differs from committed block")

          diff_path.write_text("")
          comparison.append("full_check_block=PASS")
          comparison.extend(f"placement={item}" for item in adjustments)
          comparison_path.write_text("\n".join(comparison) + "\n")
          PY

            focused_absence_checks=$(grep -Ec "^// ${check_prefix}-NOT:" \
              "$output_path/committed-test.llzk" || true)
            committed_checks=$(grep -Ev "^// ${check_prefix}-NOT:" \
              "$output_path/committed-test.llzk" | \
              grep -Ec "^// ${check_prefix}(:|-LABEL:|-NEXT:|-SAME:|-NOT:|-DAG:|-EMPTY:)" || true)
            generated_checks=$(grep -Ec "^// ${check_prefix}(:|-LABEL:|-NEXT:|-SAME:|-NOT:|-DAG:|-EMPTY:)" \
              "$output_path/generated-checks.txt" || true)

            {
              echo "head=$(git -C "$source_root" rev-parse HEAD)"
              echo "check_prefix=$check_prefix"
              echo "test=$test_file"
              echo "producer_command=result/bin/llzk-opt $* $test_file"
              echo "committed_check_lines=$committed_checks"
              echo "fresh_generated_check_lines=$generated_checks"
              echo "focused_absence_checks=$focused_absence_checks"
              echo "committed_checks_against_exact_head_output=PASS"
              echo "fresh_generated_checks_against_exact_head_output=PASS"
              echo "raw_generated_filecheck=$generated_filecheck_status"
              echo "complete_output_replay=PASS"
            } > "$output_path/replay-metadata.txt"
            full_check_results+=("$name=PASS")
          }

          prepare_head pr635 "$pr635_head"
          pr635_path="$tested_path"
          replay_pr635_suite() {
            local source_root="$1"
            local name_prefix="$2"
            replay_test "${name_prefix}-call-symbolic-felt" "$source_root" \
              test/Dialect/Function/call_with_symbolic_felt_restriction_pass.llzk \
              CHECK -split-input-file
            replay_test "${name_prefix}-struct-restrictions" "$source_root" \
              test/Dialect/Struct/struct_params_restrictions_pass.llzk \
              CHECK -split-input-file
            replay_test "${name_prefix}-struct-symbolic" "$source_root" \
              test/Dialect/Struct/struct_params_symbolic_pass.llzk \
              CHECK -split-input-file
            replay_test "${name_prefix}-include-symbolic-felt" "$source_root" \
              test/Dialect/Verif/include_with_symbolic_felt_restriction_pass.llzk \
              CHECK -split-input-file
            replay_test "${name_prefix}-expr-blocked-default" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_blocked.llzk \
              DEFAULT --enable-var-scope -llzk-flatten
            replay_test "${name_prefix}-expr-blocked-disabled" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_blocked.llzk \
              DISABLED --enable-var-scope -llzk-flatten=cleanup=disabled
            replay_test "${name_prefix}-expr-felt-typevar" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_felt_typevar_completion.llzk \
              CHECK -split-input-file -llzk-flatten
            replay_test "${name_prefix}-expr-only-default" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_only.llzk \
              DEFAULT --enable-var-scope -split-input-file -llzk-flatten
            replay_test "${name_prefix}-expr-only-disabled" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_only.llzk \
              DISABLED --enable-var-scope -split-input-file -llzk-flatten=cleanup=disabled
            replay_test "${name_prefix}-expr-partial" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_partial.llzk \
              CHECK -split-input-file -llzk-flatten
            replay_test "${name_prefix}-expr-target-local" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_target_local.llzk \
              CHECK -split-input-file '--pass-pipeline=builtin.module(llzk-flatten{cleanup=disabled})'
            replay_test "${name_prefix}-expr-typevar" "$source_root" \
              test/Transforms/Flattening/instantiate_expr_typevar_completion.llzk \
              CHECK -split-input-file -llzk-flatten
            replay_test "${name_prefix}-felt-normalization" "$source_root" \
              test/Transforms/Flattening/instantiate_func_felt_normalization.llzk \
              CHECK -split-input-file -llzk-flatten
            replay_test "${name_prefix}-funcs-pass" "$source_root" \
              test/Transforms/Flattening/instantiate_funcs_pass.llzk \
              CHECK --enable-var-scope -split-input-file -llzk-flatten -verify-diagnostics
            replay_test "${name_prefix}-nested-call" "$source_root" \
              test/Transforms/Flattening/instantiate_nested_call_template_params.llzk \
              CHECK -llzk-flatten=cleanup=disabled
            replay_test "${name_prefix}-struct-pass3" "$source_root" \
              test/Transforms/Flattening/instantiate_structs_pass_3.llzk \
              CHECK --enable-var-scope -split-input-file -llzk-flatten -verify-diagnostics
          }

          replay_pr484_suite() {
            local source_root="$1"
            local name_prefix="$2"
            replay_test "${name_prefix}-if-compute-constrain" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_if_compute_constrain.llzk \
              CHECK --enable-var-scope -split-input-file \
              --llzk-compute-constrain-to-product=root-struct=A --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-if-positive" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_if_positive.llzk \
              CHECK --enable-var-scope -split-input-file --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-if-locations" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_if_preserves_locations.llzk \
              CHECK --preserve-lines --enable-var-scope --llzk-fuse-product-control-flow \
              -mlir-print-debuginfo
            replay_test "${name_prefix}-outer" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_outer.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-outer-inner" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_outer_inner.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-single" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_single.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-structured-movement" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_structured_control_movement.llzk \
              CHECK --enable-var-scope --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-compute-suffix" "$source_root" \
              test/Transforms/FuseProductControlFlow/fuse_with_compute_suffix.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-if-effects" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_if_effects.llzk \
              CHECK --enable-var-scope -split-input-file --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-if-unknown-effect" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_if_internal_unknown_effect_in_product.llzk \
              CHECK --enable-var-scope --allow-unregistered-dialect --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-if-structure" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_if_structure.llzk \
              CHECK --enable-var-scope -split-input-file --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-inner" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_inner.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-product-source" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_product_source.llzk \
              CHECK --enable-var-scope -split-input-file --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-region-mismatch" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_region_mismatch.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-single-negative" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_single.llzk \
              CHECK --enable-var-scope --llzk-compute-constrain-to-product=root-struct=A \
              --llzk-fuse-product-control-flow
            replay_test "${name_prefix}-sink" "$source_root" \
              test/Transforms/FuseProductControlFlow/no_fuse_sink.llzk \
              CHECK --enable-var-scope --llzk-fuse-product-control-flow
          }

          prepare_head pr484 "$pr484_head"
          pr484_path="$tested_path"
          replay_pr484_suite "$pr484_path" pr484

          prepare_head pr634 "$pr634_head"
          pr634_path="$tested_path"
          replay_test pr634-call-symbolic-felt "$pr634_path" \
            test/Dialect/Function/call_with_symbolic_felt_restriction_pass.llzk \
            CHECK -split-input-file
          replay_test pr634-struct-restrictions "$pr634_path" \
            test/Dialect/Struct/struct_params_restrictions_pass.llzk \
            CHECK -split-input-file
          replay_test pr634-struct-symbolic "$pr634_path" \
            test/Dialect/Struct/struct_params_symbolic_pass.llzk \
            CHECK -split-input-file
          replay_test pr634-include-symbolic-felt "$pr634_path" \
            test/Dialect/Verif/include_with_symbolic_felt_restriction_pass.llzk \
            CHECK -split-input-file

          replay_pr635_suite "$pr635_path" pr635

          prepare_head pr637 "$pr637_head"
          pr637_path="$tested_path"
          replay_pr635_suite "$pr637_path" pr637
          replay_test pr637-function-order "$pr637_path" \
            test/Transforms/Flattening/instantiate_function_order.llzk \
            CHECK -split-input-file -llzk-flatten=cleanup=disabled
          replay_test pr637-function-reuse "$pr637_path" \
            test/Transforms/Flattening/instantiate_function_reuse.llzk \
            CHECK -split-input-file -llzk-flatten=cleanup=disabled

          prepare_head pr675 "$pr675_head"
          pr675_path="$tested_path"
          replay_test pr675-array-alias "$pr675_path" \
            test/Transforms/RedundantAndUnusedElim/array_dynamic_alias.llzk \
            CHECK --enable-var-scope -split-input-file -llzk-duplicate-read-write-elim

          if test "${#full_check_results[@]}" -eq 55; then
            full_check_blocks=PASS
          else
            full_check_blocks=FAIL
          fi
          {
            echo "pr635_head=$pr635_head"
            echo "pr637_head=$pr637_head"
            echo "pr484_head=$pr484_head"
            echo "pr634_head=$pr634_head"
            echo "pr675_head=$pr675_head"
            echo "pr484_output_replays=16"
            echo "pr634_output_replays=4"
            echo "pr635_output_replays=16"
            echo "pr637_output_replays=18"
            echo "pr675_output_replays=1"
            echo "full_output_replays=55"
            echo "full_committed_check_blocks=$full_check_blocks"
            echo "full_check_results=${full_check_results[*]}"
            echo "result=$full_check_blocks"
          } > "$OUTPUT_PATH/summary.txt"

          test "${#full_check_results[@]}" -eq 55

      - name: Upload validation evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: llzk-validation-evidence-${{ github.sha }}
          path: /tmp/llzk-validation-output
          if-no-files-found: error
