fixed:
  - Preserve target-used template expressions until staged specializations receive every required value and type binding
  - Preserve and recursively materialize explicit nested-call template arguments as enclosing parameters become concrete
  - Canonicalize equivalent felt spellings before function and struct specialization identity is computed
  - Discard failed prospective specializations without publishing their diagnostics or reusable state
