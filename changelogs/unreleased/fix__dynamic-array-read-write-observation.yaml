fixed:
  - Fix redundant read/write elimination removing an earlier array write that a later array read, extract, or aggregate-member write may observe through an alias.
  - Preserve scalar read snapshots and array value-copy semantics while retaining true struct-reference aliases across branch joins and unknown aggregate boundaries.
