fixed:
  - Fix redundant read/write elimination removing earlier array or aggregate-member writes that live aggregate reads may still observe.
  - Preserve scalar read snapshots and array value-copy semantics while retaining true struct-reference aliases when aggregate contents are known.
