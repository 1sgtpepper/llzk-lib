added:
  - Fusion for matching compute/constrain `scf.if` and `scf.for` operations in product programs

changed:
  - Rename `llzk-fuse-product-loops` to `llzk-fuse-product-control-flow` now that the pass fuses loops and conditionals
  - Preserve matching non-`product_source` attributes and combine source locations on fused `scf.if` and `scf.yield` operations; leave attribute conflicts unfused
