changed:
  - "In-place FileCheck generation now refuses dirty Git worktrees unless --allow-dirty is specified."
  - "In-place FileCheck generation now rejects use with --output."

fixed:
  - "In-place FileCheck generation now replaces the source only after successful generation and preserves unrelated text when regenerating checks for the selected prefix."
