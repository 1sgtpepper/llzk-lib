changed:
  - "In-place FileCheck generation now refuses dirty Git worktrees unless --allow-dirty is specified."

fixed:
  - "In-place FileCheck generation is now atomic and idempotent, and preserves source lines that only contain the word CHECK."
