fixed:
  - Materialize fieldless felt constants in a declared field restriction and reject explicit field or value conflicts across concrete and symbolic template arguments
  - Require symbolic template arguments to resolve to an enclosing template binding or a constant global
