fixed:
  - Verify explicit and inferred template arguments for struct types, templated function calls, and contract inclusions, accepting fieldless felt values in the required field and rejecting incompatible or ambiguous values
  - Preserve the namespace established by `include.from` when inferring template arguments from function and contract signatures
  - Require symbolic template arguments to resolve to an enclosing `poly.param` or `poly.expr` binding or a constant global
